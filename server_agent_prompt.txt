I need you to write and run a script to transcribe all .mp3 files in the current directory and its subdirectories. Since this server has a GPU, please make sure the process is hardware-accelerated.

Here are the specific steps I want you to take:
1. Install `ffmpeg` via the system package manager if it's not already installed.
2. Install `openai-whisper`. Make sure PyTorch is installed with CUDA support so Whisper can utilize the GPU for significantly faster transcription.
3. Write a bash script that iterates over all `.mp3` files in this directory recursively.
4. For each `.mp3` file, transcribe it using the Whisper CLI (e.g. `whisper "$file" --model base --output_dir "$(dirname "$file")"` - feel free to use a slightly larger model like `small` or `turbo` since we have a GPU).
5. If the transcription command finishes successfully for a file, delete the original `.mp3` file to save space.
6. Finally, make the script executable and run it so it processes all the remaining audio files.
